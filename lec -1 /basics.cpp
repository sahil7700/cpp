#include <iostream>
using namespace std;

int main(){
    cout << "hllo c++ i am sahil" << "\n" << "now use other way to\nend line" << endl;
    return 0;
}

// comments --> ctrl + /
//compile cmd -- > g++ -o code filename.cpp
// run cmd --> ./filename {which file is generated}

// here we can compile and sun together by -- >  g++ -o code filename.cpp && ./filename 